import gameStateReducer from './game.reducer';
import defaultState from './defaultState';
// const state =

describe('a test', () => {
  // const action = { type: 'UPDATE_SAME_ROUND', payload: false };

  // const output = {
  //   ...defaultState,
  //   sameRound: false,
  // };

  it('abbacadabba', () => {
    expect(1).toBe(1);
  });
});
